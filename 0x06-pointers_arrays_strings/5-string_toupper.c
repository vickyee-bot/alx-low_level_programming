#include "main.h"
/**
 * string_toupper - changes all lowercase letters of a string to uppercase
 *
 * return: always 0
 */
char *string_toupper(char *)
{
	int i = 0;

	while (*[i])
	{
		putchar(toupper(*[i]));
		i++;
	}
	return 0;
}
